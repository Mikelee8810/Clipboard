---
phase: 46.5-tauri-daemon
plan: 01
type: execute
wave: 3
depends_on: ['46.5-04']
files_modified:
  - src-tauri/crates/uc-platform/src/adapters/libp2p_network.rs
  - src-tauri/crates/uc-platform/src/adapters/network.rs
  - src-tauri/crates/uc-platform/src/adapters/mod.rs
  - src-tauri/crates/uc-bootstrap/src/assembly.rs
  - src-tauri/crates/uc-bootstrap/src/builders.rs
autonomous: true
requirements: ['R46.2-1', 'R46.2-2']
user_setup: []
must_haves:
  truths:
    - 'GUI 与 CLI 进程在完成本计划后都不再充当 pairing host；daemon 保持唯一 pairing 入口。'
    - '关闭 GUI 本地 pairing runtime 后，前端配对流程不再依赖任何本地 pairing accept loop 或本地 protocol 注册。'
    - '在 command-shell cutover 已完成的前提下，GUI 进程不再持有可用于 setup/join 的真实本地 pairing transport。'
  artifacts:
    - path: 'src-tauri/crates/uc-platform/src/adapters/libp2p_network.rs'
      provides: '显式 pairing runtime owner 配置，以及基于 owner 的 pairing service 创建/跳过逻辑。'
      min_lines: 520
    - path: 'src-tauri/crates/uc-platform/src/adapters/network.rs'
      provides: 'GUI shell 专用的 disabled pairing transport 适配器。'
      min_lines: 120
    - path: 'src-tauri/crates/uc-bootstrap/src/assembly.rs'
      provides: '按 runtime owner 装配真实 pairing transport 或 disabled pairing transport 的 composition 逻辑。'
      min_lines: 900
    - path: 'src-tauri/crates/uc-bootstrap/src/builders.rs'
      provides: 'GUI/daemon builder 向共享 wiring 显式传入 pairing runtime owner。'
      min_lines: 260
  key_links:
    - from: 'src-tauri/crates/uc-bootstrap/src/builders.rs'
      to: 'src-tauri/crates/uc-bootstrap/src/assembly.rs'
      via: 'GUI 传入 ExternalDaemon owner，daemon 传入 CurrentProcess owner。'
      pattern: 'PairingRuntimeOwner::ExternalDaemon|PairingRuntimeOwner::CurrentProcess|wire_dependencies'
    - from: 'src-tauri/crates/uc-bootstrap/src/assembly.rs'
      to: 'src-tauri/crates/uc-platform/src/adapters/libp2p_network.rs'
      via: 'Libp2pNetworkAdapter::new 的 owner 参数决定是否初始化 PairingStreamService。'
      pattern: 'Libp2pNetworkAdapter::new|PairingRuntimeOwner'
    - from: 'src-tauri/crates/uc-bootstrap/src/assembly.rs'
      to: 'src-tauri/crates/uc-platform/src/adapters/network.rs'
      via: 'GUI shell 的 NetworkPorts.pairing 改绑 DisabledPairingTransport 而不是真实 libp2p transport。'
      pattern: 'DisabledPairingTransport|pairing:'
---

<objective>
把“谁拥有本地 pairing runtime”从隐式事实改成 composition-root 级别的显式配置，并在 GUI builder 上切断真实本地 pairing transport。

Purpose: 让 GUI 不再通过“忽略本地事件”假装 daemon-owned，而是在 `uc-bootstrap` + `uc-platform` 层真正不创建本地 pairing runtime。
Output: pairing runtime owner 配置、GUI disabled pairing transport 装配、以及 GUI/daemon owner 差异化测试。
</objective>

<execution_context>
Use the standard execute-phase workflow already configured for this repository.
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46.5-tauri-daemon/46.5-CONTEXT.md
@.planning/phases/46.5-tauri-daemon/46.5-RESEARCH.md
@.planning/phases/46.5-tauri-daemon/46.5-VALIDATION.md
@src-tauri/crates/uc-app/src/deps.rs
@src-tauri/crates/uc-platform/src/adapters/libp2p_network.rs
@src-tauri/crates/uc-platform/src/adapters/network.rs
@src-tauri/crates/uc-bootstrap/src/assembly.rs
@src-tauri/crates/uc-bootstrap/src/builders.rs

<interfaces>
From `src-tauri/crates/uc-app/src/deps.rs`:
```rust
pub struct NetworkPorts {
    pub clipboard: Arc<dyn ClipboardTransportPort>,
    pub peers: Arc<dyn PeerDirectoryPort>,
    pub pairing: Arc<dyn PairingTransportPort>,
    pub events: Arc<dyn NetworkEventPort>,
    pub file_transfer: Arc<dyn FileTransportPort>,
}
```

From `src-tauri/crates/uc-platform/src/adapters/libp2p_network.rs`:

```rust
pub struct Libp2pNetworkAdapter {
    pairing_service: Mutex<Option<PairingStreamService>>,
}

impl Libp2pNetworkAdapter {
    pub fn new(...) -> Result<Self>;
    pub fn spawn_swarm(&self) -> Result<()>;
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 uc-platform 显式表达本地 pairing runtime owner 并为 GUI 提供 disabled pairing transport</name>
  <files>
    src-tauri/crates/uc-platform/src/adapters/libp2p_network.rs,
    src-tauri/crates/uc-platform/src/adapters/network.rs,
    src-tauri/crates/uc-platform/src/adapters/mod.rs
  </files>
  <read_first>
    - src-tauri/crates/uc-platform/src/adapters/libp2p_network.rs
    - src-tauri/crates/uc-platform/src/adapters/network.rs
    - src-tauri/crates/uc-platform/src/adapters/mod.rs
    - src-tauri/crates/uc-app/src/deps.rs
    - src-tauri/crates/uc-core/src/ports/mod.rs
    - .planning/phases/46.5-tauri-daemon/46.5-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: 传入 `PairingRuntimeOwner::ExternalDaemon` 时，`spawn_swarm()` 不创建 `PairingStreamService`，不会启动 pairing accept loop，且不会注册 `/clipboard/pairing-stream/1.0.0` handler/protocol。
    - Test 2: 传入 `PairingRuntimeOwner::CurrentProcess` 时，现有本地 pairing 行为保持可用，`pairing_service` 仍会被初始化，并继续注册 `/clipboard/pairing-stream/1.0.0`。
    - Test 3: `DisabledPairingTransport` 的 `open_pairing_session`、`send_pairing_on_session`、`close_pairing_session`、`unpair_device` 全部返回固定错误字符串 `local pairing runtime is disabled in this process`。
  </behavior>
  <action>
    1. 在 `src-tauri/crates/uc-platform/src/adapters/network.rs` 中新增 `PairingRuntimeOwner` 枚举，固定包含两个值：
       - `CurrentProcess`
       - `ExternalDaemon`
    2. 同文件新增 `DisabledPairingTransport`，实现 `PairingTransportPort`，四个方法全部返回 `anyhow!("local pairing runtime is disabled in this process")`；不要静默 `Ok(())`。
    3. 在 `src-tauri/crates/uc-platform/src/adapters/libp2p_network.rs` 中给 `Libp2pNetworkAdapter` 增加 `pairing_runtime_owner: PairingRuntimeOwner` 字段，并把 `Libp2pNetworkAdapter::new(...)` 的最后一个参数改成这个 owner。
    4. 在 `spawn_swarm()` 中仅当 owner 为 `PairingRuntimeOwner::CurrentProcess` 时才执行下面四步：
       - 创建 `PairingStreamService::new(...)`
       - 调用 `pairing_service.spawn_accept_loop()`
       - 注册 `/clipboard/pairing-stream/1.0.0` 对应的 pairing stream handler/protocol
       - 把 `self.pairing_service` 写成 `Some(pairing_service)`
       当 owner 为 `ExternalDaemon` 时，必须跳过这四步，并保持 `self.pairing_service == None`。
    5. 保留文件传输、剪贴板、peer discovery 的既有启动逻辑，不要因为禁用 pairing runtime 而影响 `FileTransferService` 或 `stream_control` 初始化。
    6. 在 `src-tauri/crates/uc-platform/src/adapters/mod.rs` 里导出 `PairingRuntimeOwner` 和 `DisabledPairingTransport`，供 `uc-bootstrap` 装配使用。
    7. 在 `libp2p_network.rs` 的现有测试区补两组明确命名的测试：
       - `pairing_runtime_disabled_does_not_initialize_pairing_service`
       - `pairing_runtime_disabled_does_not_register_pairing_protocol`
       - `pairing_runtime_current_process_initializes_pairing_service`
       并在 `network.rs` 补 `disabled_pairing_transport_returns_fixed_error`。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test -p uc-platform pairing_runtime -- --test-threads=1 && cargo test -p uc-platform disabled_pairing_transport_returns_fixed_error -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg "enum PairingRuntimeOwner|CurrentProcess|ExternalDaemon" src-tauri/crates/uc-platform/src/adapters/network.rs -n` 返回 owner 枚举定义。
    - `rg "struct DisabledPairingTransport|local pairing runtime is disabled in this process" src-tauri/crates/uc-platform/src/adapters/network.rs -n` 返回 disabled transport 及固定错误字符串。
    - `rg "pairing_runtime_owner|PairingRuntimeOwner::CurrentProcess|PairingRuntimeOwner::ExternalDaemon" src-tauri/crates/uc-platform/src/adapters/libp2p_network.rs -n` 返回 adapter owner 字段与分支。
    - `rg "pairing_runtime_disabled_does_not_initialize_pairing_service|pairing_runtime_disabled_does_not_register_pairing_protocol|pairing_runtime_current_process_initializes_pairing_service|disabled_pairing_transport_returns_fixed_error" src-tauri/crates/uc-platform/src/adapters/libp2p_network.rs src-tauri/crates/uc-platform/src/adapters/network.rs -n` 返回 4 个测试名。
    - `rg "/clipboard/pairing-stream/1.0.0|register.*pairing|pairing.*protocol" src-tauri/crates/uc-platform/src/adapters/libp2p_network.rs -n` 返回 protocol 注册逻辑及其 owner gating。
    - `cd src-tauri && cargo test -p uc-platform pairing_runtime -- --test-threads=1` 退出码为 0。
  </acceptance_criteria>
  <done>
    - `uc-platform` 具备显式 pairing runtime owner 配置。
    - GUI shell 可以拿到一个明确失败的 pairing transport，而不是继续持有真实 libp2p pairing transport。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 uc-bootstrap 把 GUI/daemon 的 pairing ownership 显式传入共享 wiring</name>
  <files>
    src-tauri/crates/uc-bootstrap/src/assembly.rs,
    src-tauri/crates/uc-bootstrap/src/builders.rs
  </files>
  <read_first>
    - src-tauri/crates/uc-bootstrap/src/assembly.rs
    - src-tauri/crates/uc-bootstrap/src/builders.rs
    - src-tauri/crates/uc-platform/src/adapters/network.rs
    - src-tauri/crates/uc-platform/src/adapters/libp2p_network.rs
    - src-tauri/crates/uc-app/src/deps.rs
    - .planning/phases/46.5-tauri-daemon/46.5-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `build_gui_app()` 走 shared composition root 时向 `Libp2pNetworkAdapter::new(...)` 传 `PairingRuntimeOwner::ExternalDaemon`。
    - Test 2: `build_daemon_app()` 仍向 `Libp2pNetworkAdapter::new(...)` 传 `PairingRuntimeOwner::CurrentProcess`。
    - Test 3: GUI 侧 `NetworkPorts.pairing` 绑定 `DisabledPairingTransport`，daemon 侧 `NetworkPorts.pairing` 仍绑定真实 `libp2p_network.clone()`。
  </behavior>
  <action>
    1. 在 `src-tauri/crates/uc-bootstrap/src/assembly.rs` 中，把 `wire_dependencies(...)` / `wire_dependencies_with_identity_store(...)` / 内部平台层装配函数改成显式接收 `pairing_runtime_owner: PairingRuntimeOwner`。
    2. 创建 `Libp2pNetworkAdapter` 时，把这个 owner 作为最后一个参数传给 `Libp2pNetworkAdapter::new(...)`。
    3. 组装 `NetworkPorts` 时使用固定规则：
       - 当 owner 为 `CurrentProcess`：`pairing: libp2p_network.clone()`
       - 当 owner 为 `ExternalDaemon`：`pairing: Arc::new(DisabledPairingTransport)`
       其余 `clipboard`、`peers`、`events`、`file_transfer` 继续绑定真实 `libp2p_network.clone()`。
    4. 在 `src-tauri/crates/uc-bootstrap/src/builders.rs` 中固定 builder owner：
       - `build_gui_app()` -> `PairingRuntimeOwner::ExternalDaemon`
       - `build_daemon_app()` -> `PairingRuntimeOwner::CurrentProcess`
       - `build_cli_context()` / `build_cli_context_with_profile()` -> `PairingRuntimeOwner::ExternalDaemon`
    5. 在 `builders.rs` 或 `assembly.rs` 增加明确命名测试：
       - `gui_builder_uses_external_daemon_pairing_owner`
       - `daemon_builder_uses_current_process_pairing_owner`
       - `gui_network_ports_pairing_is_disabled_transport`
       测试不需要启动真实 daemon，但必须直接断言 owner 传递和 pairing port 装配结果。
  </action>
  <verify>
    <automated>cd src-tauri && cargo test -p uc-bootstrap pairing_owner -- --test-threads=1 && cargo test -p uc-bootstrap gui_network_ports_pairing_is_disabled_transport -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg "PairingRuntimeOwner::ExternalDaemon|PairingRuntimeOwner::CurrentProcess" src-tauri/crates/uc-bootstrap/src/builders.rs src-tauri/crates/uc-bootstrap/src/assembly.rs -n` 返回 GUI/daemon/CLI owner 装配。
    - `rg "DisabledPairingTransport|pairing: .*DisabledPairingTransport|pairing: libp2p_network.clone" src-tauri/crates/uc-bootstrap/src/assembly.rs -n` 返回 GUI/daemon pairing port 差异化绑定。
    - `rg "gui_builder_uses_external_daemon_pairing_owner|daemon_builder_uses_current_process_pairing_owner|gui_network_ports_pairing_is_disabled_transport" src-tauri/crates/uc-bootstrap/src/builders.rs src-tauri/crates/uc-bootstrap/src/assembly.rs -n` 返回 3 个测试名。
    - `cd src-tauri && cargo test -p uc-bootstrap pairing_owner -- --test-threads=1` 退出码为 0。
  </acceptance_criteria>
  <done>
    - GUI/daemon/CLI 对本地 pairing runtime ownership 的选择变成显式装配事实。
    - GUI 即使继续共享同一套 composition root，也不会再偷偷持有真实本地 pairing transport。
  </done>
</task>

</tasks>

<verification>
`cd src-tauri && cargo test -p uc-platform pairing_runtime -- --test-threads=1`
`cd src-tauri && cargo test -p uc-bootstrap pairing_owner -- --test-threads=1`
</verification>

<success_criteria>
在 setup/pairing command shell 已完成 daemon-only 收口之后，GUI/CLI 对本地 pairing runtime 的拥有权才被结构化切断；daemon 仍保留当前本地 pairing runtime 能力，且不需要在 `uc-tauri::bootstrap::wiring.rs` 靠“忽略本地事件”来模拟 daemon ownership。
</success_criteria>

<output>
完成后创建 `.planning/phases/46.5-tauri-daemon/46.5-01-SUMMARY.md`，总结 pairing runtime owner、GUI disabled pairing transport 与共享 wiring 的最终形态。
</output>
