---
phase: 95
plan: 05
type: tdd
wave: 2
depends_on: [95-03]
files_modified:
  - src/components/setting/RestartBanner.tsx
  - src/components/setting/LanOnlyDisclosure.tsx
  - src/components/setting/__tests__/RestartBanner.test.tsx
  - src/components/setting/__tests__/LanOnlyDisclosure.test.tsx
autonomous: true
requirements: [NETSET-05, NETSET-06]
tags: [frontend, components, lan-only, restart-banner, popover]
must_haves:
  truths:
    - "RestartBanner 在 visible=false 时不渲染（节点不挂）；visible=true 时渲染 role=status + RefreshCw + 立即重启 Button"
    - "RestartBanner error sub-state 渲染 role=alert 文本 + 重试 Button + dismiss X icon"
    - "LanOnlyDisclosure 是 click-only Popover（per D-C1，禁用 hover Tooltip）；trigger 用 <button> + aria-haspopup=dialog"
    - "LanOnlyDisclosure 4 类外网请求 PopoverContent 完整渲染（rendezvous + otlp + pkarr + autoUpdate 各一对 title+description）"
    - "两个组件不复用 shadcn Alert（D-A1），不引入 sonner toast，不维护 lanOnly 字段"
  artifacts:
    - path: "src/components/setting/RestartBanner.tsx"
      provides: "持久 inline RestartBanner 组件（D-A1, D-A2, D-A3, D-B3）"
      contains: "interface RestartBannerProps"
    - path: "src/components/setting/LanOnlyDisclosure.tsx"
      provides: "info icon Popover trigger + 4 类外网请求清单（D-C1, D-C2）"
      contains: "aria-haspopup=\"dialog\""
    - path: "src/components/setting/__tests__/RestartBanner.test.tsx"
      provides: "RestartBanner 渲染 + 交互 + a11y 单元测试"
      contains: "describe('RestartBanner"
    - path: "src/components/setting/__tests__/LanOnlyDisclosure.test.tsx"
      provides: "LanOnlyDisclosure click-only Popover + 4 类清单 + a11y 单元测试"
      contains: "describe('LanOnlyDisclosure"
  key_links:
    - from: "RestartBanner Button onClick"
      to: "Plan 06 NetworkSection 注入的 onRestart prop"
      via: "Plan 06 NetworkSection 把 invokeWithTrace('restart_app') 包装成 props.onRestart"
      pattern: "onRestart\\(\\)"
    - from: "LanOnlyDisclosure i18n keys"
      to: "Plan 03 settings.sections.network.lanOnly.disclosure.*"
      via: "useTranslation + t(key)"
      pattern: "settings\\.sections\\.network\\.lanOnly\\.disclosure"
---

<objective>
实装 Phase 95 两个全新组件：

1. **RestartBanner**（D-A1 / D-A2 / D-A3 / D-B3）：持久 inline 重启通知，**不复用** shadcn `Alert`、**不** 用 sonner toast。Props 接受 `{ visible, onRestart, loading?, error?, onDismissError? }`，由 Plan 06 NetworkSection 在 SettingGroup 内嵌入。
2. **LanOnlyDisclosure**（D-C1 / D-C2）：info-icon **click-only** Popover trigger，含 4 类外网请求清单（rendezvous / otlp / pkarr / autoUpdate）；用 `<button>` + `aria-haspopup="dialog"` 不是裸 icon。

两个组件 **完全 i18n-driven**（依赖 Plan 03 已 ship 的双语文案），自身不硬编码任何 LAN-only 词汇 — 所以 RestartBanner 后续 Phase 96/97 可复用（D-A1 备注：Banner 文案通过 props 注入；本 plan 让 Banner 接受 i18n 路径或直接接受 message string，由 NetworkSection 在 Plan 06 注入）。

**Purpose:**
- Pitfall 10 防御核心：持久 inline 通知 + 「立即重启」CTA + debounce（debounce 在 Plan 06 NetworkSection 内做）+ 三态视觉（只靠 Banner 出现/消失表达 — D-A3）
- Pitfall 5 防御：info-icon Popover 4 类外网请求清单（D-C2）— 双语文案已在 Plan 03 写定，本 plan 仅做渲染

**Output:** 两个组件 + 完整测试（RTL + jsdom + vitest）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/095-networksection-ux/095-CONTEXT.md
@.planning/phases/095-networksection-ux/095-PATTERNS.md
@.planning/phases/095-networksection-ux/095-UI-SPEC.md
@src/components/ui/popover.tsx
@src/components/ui/button.tsx
@src/i18n/locales/zh-CN.json
@src/i18n/locales/en-US.json

<interfaces>
<!-- RestartBanner Props 契约（Plan 06 NetworkSection 必须按此调用） -->

```typescript
export interface RestartBannerProps {
  visible: boolean
  onRestart: () => Promise<void>
  loading?: boolean
  error?: string | null
  onDismissError?: () => void
}
```

<!-- LanOnlyDisclosure 无 props（i18n-driven 自包含） -->

```typescript
export function LanOnlyDisclosure(): JSX.Element
```

<!-- shadcn Popover 已 ship 的导出（src/components/ui/popover.tsx） -->
import { Popover, PopoverTrigger, PopoverContent } from '@/components/ui/popover'
// PopoverContent 默认 align="center" sideOffset={8} w-72 rounded-xl border bg-popover p-3

<!-- shadcn Button 已 ship 的导出（src/components/ui/button.tsx） -->
import { Button } from '@/components/ui/button'
// variant: "default" | "outline" | "ghost" | ...; size: "default" | "sm" | "icon"

<!-- lucide-react icons 已可用 -->
import { RefreshCw, X, Info } from 'lucide-react'
</interfaces>

<analog_excerpt>
<!-- 完整 RestartBanner Layout — UI-SPEC.md `## Layout Contract` 给出的 DOM 结构 -->
```jsx
<div
  role="status"
  aria-live="polite"
  className="flex items-start gap-2 px-4 py-3 bg-accent/40 border-b border-border/40 rounded-none"
>
  <RefreshCw className="size-4 text-foreground mt-0.5 shrink-0" />
  <div className="flex-1 space-y-1">
    <p className="text-sm text-foreground">{message}</p>
    {error && (
      <p role="alert" className="text-xs text-destructive">{error}</p>
    )}
  </div>
  <div className="ml-auto flex items-center gap-2">
    {!error ? (
      <Button variant="default" size="sm" onClick={onRestart} disabled={loading}>
        {loading ? t('...restartingButton') : t('...restartButton')}
      </Button>
    ) : (
      <>
        <Button variant="outline" size="sm" onClick={onRestart} disabled={loading}>
          {t('...retryButton')}
        </Button>
        <Button variant="ghost" size="icon" aria-label={t('...dismissAriaLabel')}
          onClick={onDismissError}>
          <X className="size-3.5" />
        </Button>
      </>
    )}
  </div>
</div>
```

<!-- LanOnlyDisclosure 完整结构 — UI-SPEC.md `## Layout Contract` -->
```jsx
<Popover>
  <PopoverTrigger asChild>
    <button
      type="button"
      aria-label={t('settings.sections.network.lanOnly.infoIconAriaLabel')}
      aria-haspopup="dialog"
      className="inline-flex items-center justify-center rounded-md p-1
                 text-muted-foreground hover:text-foreground hover:bg-accent"
    >
      <Info className="size-3.5" />
    </button>
  </PopoverTrigger>
  <PopoverContent align="start" sideOffset={8}>
    <div className="space-y-3">
      <div>
        <p className="text-sm font-medium">{t('...disclosure.title')}</p>
        <p className="text-xs text-muted-foreground mt-1">{t('...disclosure.intro')}</p>
      </div>
      <div className="space-y-2">
        {(['rendezvous', 'otlp', 'pkarr', 'autoUpdate'] as const).map((key) => (
          <div key={key} className="space-y-1">
            <p className="text-sm font-medium">{t(`...disclosure.${key}.title`)}</p>
            <p className="text-xs text-muted-foreground leading-snug">
              {t(`...disclosure.${key}.description`)}
            </p>
          </div>
        ))}
      </div>
    </div>
  </PopoverContent>
</Popover>
```
</analog_excerpt>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — RestartBanner 渲染、交互、a11y 测试</name>
  <read_first>
    - `.planning/phases/095-networksection-ux/095-UI-SPEC.md` §`## Layout Contract` + §`## Accessibility Contract`
    - `.planning/phases/095-networksection-ux/095-PATTERNS.md` §`src/components/setting/RestartBanner.tsx`
    - `src/components/setting/__tests__/AboutSection.test.tsx`（如存在 — RTL setup pattern；如不存在则参考 `src/contexts/__tests__/SettingContext.theme.test.tsx`）
    - `src/components/ui/button.tsx`（确认 Button props variant + size 类型）
    - `src/i18n/locales/zh-CN.json` settings.sections.network.restartBanner（Plan 03 已 ship — 验证 i18n keys 形态）
  </read_first>
  <files>src/components/setting/__tests__/RestartBanner.test.tsx</files>
  <behavior>
    Test 1（visible=false 不渲染）: `render(<RestartBanner visible={false} onRestart={vi.fn()} />)`，期望 `screen.queryByRole('status')` 返回 null
    Test 2（visible=true 渲染主信息 + role=status）: 期望 `screen.getByRole('status')` 存在，文本含 i18n message ("需要重启应用以使 LAN-only 模式更改生效。" 或英文)；aria-live="polite"
    Test 3（"立即重启" Button 点击触发 onRestart）: `userEvent.click(screen.getByRole('button', { name: /立即重启|Restart now/ }))`，期望 onRestart mock 被调一次
    Test 4（loading=true 时 Button 禁用 + 文本变 "正在重启…"）: 期望 Button disabled=true + getByRole('button', { name: /正在重启|Restarting…/ })
    Test 5（error 不为 null 时显示 role=alert 错误文本）: `<RestartBanner visible error="自动重启失败"`，期望 `screen.getByRole('alert')` 文本含 "自动重启失败"
    Test 6（error 状态显示 重试 Button 与 dismiss X icon）: 期望两个按钮都存在；点击 dismiss X 触发 onDismissError mock
    Test 7（visible=true 时含 lucide RefreshCw icon）: 期望 SVG 节点存在 — 用 `container.querySelector('svg')` 或测 className 含 lucide 标识
    Test 8（不复用 shadcn Alert — 静态结构 fence）: render 后 `container.querySelector('[data-slot="alert"]')` 应为 null（shadcn Alert 通常带 data-slot）
  </behavior>
  <action>
    1. 创建 `src/components/setting/__tests__/RestartBanner.test.tsx`，参考现有 RTL test 模式
    2. 包装 i18n provider（i18next 已在项目内全局初始化 — 测试中可直接 import 真实 i18n config 或用 i18next inline init）
    3. 使用 `import '@testing-library/jest-dom'` 启用 `.toBeInTheDocument()` 等 matchers
    4. 测试 1-8 完整实装。注意：
       - i18n key 路径：`settings.sections.network.restartBanner.message` / `restartButton` / `restartingButton` / `errorMessage` / `retryButton` / `dismissAriaLabel`
       - 因测试运行时 i18n 默认英文（en-US），message 文本会是 "Restart the app to apply..."；但测试用正则 `/Restart now|立即重启/` 兼容两种
       - Test 4 注意 Button disabled prop —— shadcn Button 用 `disabled` HTML attr，testing-library 的 `toBeDisabled()` 即可
    5. 运行 `pnpm test --run src/components/setting/__tests__/RestartBanner.test.tsx` 期望 **失败**（RestartBanner 文件还不存在 — 报 import error）
    6. 提交：`test(95-05): RED — RestartBanner 渲染、交互、a11y 测试`
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/clipboard/lagos && pnpm test --run src/components/setting/__tests__/RestartBanner.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/setting/__tests__/RestartBanner.test.tsx` 文件存在
    - 文件含 `describe('RestartBanner` 或类似 block
    - 文件含至少 8 个 `it(` / `test(` 语句
    - 文件至少出现 8 处 `RestartBanner`、3 处 `onRestart`、3 处 `onDismissError`
    - 文件含 `'@testing-library/jest-dom'` import
    - 文件含 `'@testing-library/user-event'` 或 `fireEvent` import
    - 运行 `pnpm test --run src/components/setting/__tests__/RestartBanner.test.tsx` 退出码非 0（RED — 因 RestartBanner.tsx 不存在）
    - `git log --oneline -1` 标题以 `test(95-05):` 开头
  </acceptance_criteria>
  <done>测试文件提交，运行失败（import 报错或全部 fail）。</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — 实装 RestartBanner 组件</name>
  <read_first>
    - Task 1 创建的失败测试
    - `.planning/phases/095-networksection-ux/095-UI-SPEC.md` §`## Layout Contract` + §`## Color` + §`## Accessibility Contract`
    - `.planning/phases/095-networksection-ux/095-PATTERNS.md` §`src/components/setting/RestartBanner.tsx`
    - `src/components/ui/button.tsx`（Button signature）
  </read_first>
  <files>src/components/setting/RestartBanner.tsx</files>
  <action>
    创建 `src/components/setting/RestartBanner.tsx`，按 UI-SPEC `## Layout Contract` 完整实装：

    ```typescript
    import { useTranslation } from 'react-i18next'
    import { RefreshCw, X } from 'lucide-react'
    import { Button } from '@/components/ui/button'

    /**
     * RestartBanner — Phase 95 持久 inline 重启通知。
     *
     * Per CONTEXT D-A1: 不复用 shadcn Alert，不用 sonner toast。
     * Per CONTEXT D-A2: 由父组件（NetworkSection）嵌入到 SettingGroup 内部、Switch 上方。
     * Per CONTEXT D-A3: 三态视觉只靠 Banner 出现/消失表达，Switch 自身样式不动。
     * Per CONTEXT D-B3: app.restart() 失败时通过 error prop 渲染 inline error，不抛 toast。
     *
     * Reusability: 文案完全通过 i18n key 注入，未来 Phase 96/97 可复用本组件
     * （文案 key 由父注入；本 plan 暂硬编码 settings.sections.network.restartBanner.* 路径，
     * 后续若 Phase 96/97 复用可重构为 props.messageKey 注入 — 留待真有需求时做）。
     */
    export interface RestartBannerProps {
      visible: boolean
      onRestart: () => Promise<void>
      loading?: boolean
      error?: string | null
      onDismissError?: () => void
    }

    export function RestartBanner({
      visible,
      onRestart,
      loading = false,
      error = null,
      onDismissError,
    }: RestartBannerProps) {
      const { t } = useTranslation()

      if (!visible) return null

      return (
        <div
          role="status"
          aria-live="polite"
          className="flex items-start gap-2 px-4 py-3 bg-accent/40 border-b border-border/40 rounded-none"
        >
          <RefreshCw className="size-4 text-foreground mt-0.5 shrink-0" aria-hidden="true" />
          <div className="flex-1 space-y-1">
            <p className="text-sm text-foreground">
              {t('settings.sections.network.restartBanner.message')}
            </p>
            {error && (
              <p role="alert" className="text-xs text-destructive">
                {error}
              </p>
            )}
          </div>
          <div className="ml-auto flex items-center gap-2">
            {!error ? (
              <Button
                variant="default"
                size="sm"
                onClick={() => {
                  void onRestart()
                }}
                disabled={loading}
              >
                {loading
                  ? t('settings.sections.network.restartBanner.restartingButton')
                  : t('settings.sections.network.restartBanner.restartButton')}
              </Button>
            ) : (
              <>
                <Button
                  variant="outline"
                  size="sm"
                  onClick={() => {
                    void onRestart()
                  }}
                  disabled={loading}
                >
                  {t('settings.sections.network.restartBanner.retryButton')}
                </Button>
                <Button
                  variant="ghost"
                  size="icon"
                  aria-label={t('settings.sections.network.restartBanner.dismissAriaLabel')}
                  onClick={onDismissError}
                >
                  <X className="size-3.5" aria-hidden="true" />
                </Button>
              </>
            )}
          </div>
        </div>
      )
    }

    export default RestartBanner
    ```

    **关键约束（Pitfall 防御）：**
    1. **不** import `Alert` from `@/components/ui/alert`（即便项目有也不用 — D-A1 锁定）
    2. **不** import `toast` from `sonner` 或 `react-hot-toast`（D-A1 锁定）
    3. `<button>` 的 onClick 包 `void` 让 Promise 不破 React 类型
    4. RefreshCw + X 都加 `aria-hidden="true"`（图标装饰，文本承担语义）

    运行 `pnpm test --run src/components/setting/__tests__/RestartBanner.test.tsx` 期望 8/8 PASS。

    提交：`feat(95-05a): GREEN — RestartBanner 持久 inline 重启通知组件`
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/clipboard/lagos && pnpm test --run src/components/setting/__tests__/RestartBanner.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/setting/RestartBanner.tsx` 存在
    - 文件含 `export interface RestartBannerProps`
    - 文件含 `visible: boolean`
    - 文件含 `onRestart: () => Promise<void>`
    - 文件含 `role="status"`
    - 文件含 `aria-live="polite"`
    - 文件含 `RefreshCw`
    - 文件含 `bg-accent/40`（per UI-SPEC color contract）
    - 文件含 `t('settings.sections.network.restartBanner.message')`
    - 文件不含 `from '@/components/ui/alert'`（D-A1 fence）
    - 文件不含 `from 'sonner'`（D-A1 fence）
    - 文件不含 `react-hot-toast`（D-A1 fence）
    - 文件不含 `'lanOnly'` 字符串（per cross-phase reusability — 文案 i18n-driven）
    - `pnpm test --run src/components/setting/__tests__/RestartBanner.test.tsx` 退出码 0（8/8 PASS）
    - `git log --oneline -1` 标题以 `feat(95-05a):` 开头
  </acceptance_criteria>
  <done>RestartBanner 实装 + 8/8 测试 PASS + Alert/toast fence 落实。</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: RED+GREEN — LanOnlyDisclosure 测试 + 实装（合并 RED→GREEN 为 click-only Popover 较简单）</name>
  <read_first>
    - `.planning/phases/095-networksection-ux/095-UI-SPEC.md` §`## Layout Contract` LanOnlyDisclosure 段 + §`## Interaction Contract`（"User hovers info-icon → NO action"）
    - `.planning/phases/095-networksection-ux/095-PATTERNS.md` §`src/components/setting/LanOnlyDisclosure.tsx`
    - `src/components/ui/popover.tsx`（Radix Popover signature）
    - `src/i18n/locales/zh-CN.json` settings.sections.network.lanOnly.disclosure.*（Plan 03 已 ship）
  </read_first>
  <files>src/components/setting/LanOnlyDisclosure.tsx, src/components/setting/__tests__/LanOnlyDisclosure.test.tsx</files>
  <action>
    **Step A — 写测试 `src/components/setting/__tests__/LanOnlyDisclosure.test.tsx`：**

    ```typescript
    Test 1（trigger 是 <button> 含 aria-haspopup="dialog"）: render 后 getByRole('button', { name: /查看 LAN-only|View the list/ }) 存在；该 button 含 aria-haspopup="dialog"
    Test 2（trigger 含 lucide Info icon）: button 内含 SVG（aria-hidden="true"）
    Test 3（hover 不打开 Popover — D-C1）: fireEvent.mouseEnter 后 PopoverContent 不应渲染（由于 Radix Popover 默认就是 click-only — 单元测试已天然 PASS，仅作 fence）
    Test 4（click 打开 Popover）: userEvent.click trigger 后 getByRole('dialog') 存在
    Test 5（PopoverContent 含 4 类外网请求标题）: 打开后 query 文本 `首次配对 rendezvous|First-pairing rendezvous`、`OTLP 遥测|OTLP telemetry`、`pkarr DHT|pkarr DHT NodeId resolution`、`自动更新 GitHub 检查|Auto-update GitHub check` 都存在
    Test 6（Esc 关闭 Popover）: 打开后 keydown Escape，期望 dialog 不再可见
    Test 7（不含 lanOnly 字段引用 fence）: render 测试中无 lanOnly 字段访问 — 此测试静态化，由 acceptance grep 完成
    ```

    使用 i18n: 用真实 i18next config（与 Test 1 同 setup）；测试中通过 i18n.changeLanguage 切到 zh-CN 验证中文文案 + 切到 en-US 验证英文。

    **Step B — 实装 `src/components/setting/LanOnlyDisclosure.tsx`：**

    ```typescript
    import { useTranslation } from 'react-i18next'
    import { Info } from 'lucide-react'
    import { Popover, PopoverTrigger, PopoverContent } from '@/components/ui/popover'

    /**
     * LanOnlyDisclosure — info icon click-only Popover, lists 4 类仍走外网的请求。
     *
     * Per CONTEXT D-C1: 用 Popover 不用 hover Tooltip（hover 容纳量不够）。
     * Per CONTEXT D-C2: 4 类外网请求文案最终敲定（Phase 97 反向复制基准）。
     *
     * 文案完全来自 i18n（Plan 03 已 ship 双语），本组件无任何硬编码 LAN-only 字串
     * — 但因为内容专门讲 LAN-only 边界，组件名带 LanOnly 前缀（语义清晰）。
     */
    const DISCLOSURE_KEYS = ['rendezvous', 'otlp', 'pkarr', 'autoUpdate'] as const

    export function LanOnlyDisclosure() {
      const { t } = useTranslation()

      return (
        <Popover>
          <PopoverTrigger asChild>
            <button
              type="button"
              aria-label={t('settings.sections.network.lanOnly.infoIconAriaLabel')}
              aria-haspopup="dialog"
              className="inline-flex items-center justify-center rounded-md p-1 text-muted-foreground hover:text-foreground hover:bg-accent focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring/50"
            >
              <Info className="size-3.5" aria-hidden="true" />
            </button>
          </PopoverTrigger>
          <PopoverContent align="start" sideOffset={8}>
            <div className="space-y-3">
              <div>
                <p className="text-sm font-medium">
                  {t('settings.sections.network.lanOnly.disclosure.title')}
                </p>
                <p className="text-xs text-muted-foreground mt-1">
                  {t('settings.sections.network.lanOnly.disclosure.intro')}
                </p>
              </div>
              <div className="space-y-2">
                {DISCLOSURE_KEYS.map((key) => (
                  <div key={key} className="space-y-1">
                    <p className="text-sm font-medium">
                      {t(`settings.sections.network.lanOnly.disclosure.${key}.title`)}
                    </p>
                    <p className="text-xs text-muted-foreground leading-snug">
                      {t(`settings.sections.network.lanOnly.disclosure.${key}.description`)}
                    </p>
                  </div>
                ))}
              </div>
            </div>
          </PopoverContent>
        </Popover>
      )
    }

    export default LanOnlyDisclosure
    ```

    **关键约束：**
    1. trigger 必须是 `<button type="button">` 且 `aria-haspopup="dialog"`（D-C1 + a11y）
    2. **不** 用 `<Tooltip>` 或 `onMouseEnter` open Popover（D-C1：仅 click）
    3. DISCLOSURE_KEYS 写成 `as const` 让 TypeScript 推断 readonly tuple
    4. 不引用任何 lanOnly 字段（仅文案 i18n key 含 lanOnly 路径，那是 i18n key 规范，不算字段镜像）

    运行 `pnpm test --run src/components/setting/__tests__/LanOnlyDisclosure.test.tsx` 期望 6+/6+ PASS。

    提交：`feat(95-05b): GREEN — LanOnlyDisclosure click-only Popover 4 类外网请求清单`
  </action>
  <verify>
    <automated>cd /Users/mark/conductor/workspaces/clipboard/lagos && pnpm test --run src/components/setting/__tests__/LanOnlyDisclosure.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/setting/LanOnlyDisclosure.tsx` 存在
    - 文件含 `aria-haspopup="dialog"`
    - 文件含 `<Info`（lucide icon）
    - 文件含 `Popover`、`PopoverTrigger`、`PopoverContent`（三个组件 import）
    - 文件含 `'rendezvous', 'otlp', 'pkarr', 'autoUpdate'`（4 类 keys 数组）
    - 文件不含 `Tooltip`（D-C1 fence — 不用 hover Tooltip）
    - 文件不含 `onMouseEnter|onHover`（fence — Popover 必须是 click-only）
    - `src/components/setting/__tests__/LanOnlyDisclosure.test.tsx` 存在
    - 测试文件含至少 6 个 `it(` / `test(`
    - `pnpm test --run src/components/setting/__tests__/LanOnlyDisclosure.test.tsx` 退出码 0（≥6 PASS）
    - 全工程 `grep -rE "lanOnly[^A-Za-z]" src/components/setting/RestartBanner.tsx src/components/setting/LanOnlyDisclosure.tsx` 返回 0 匹配（前两个组件无 lanOnly 字段镜像；i18n key 是字符串文字，不算字段，不会被匹配 — 因为 i18n key 是 `lanOnly.disclosure` 含 `.`，正则 `lanOnly[^A-Za-z]` 会匹配 `.` —— 此 grep 应该有 N>0 命中。**改用** `grep -rE "(let|const|var)[[:space:]]+lanOnly|lanOnly[[:space:]]*[:=]" src/components/setting/RestartBanner.tsx src/components/setting/LanOnlyDisclosure.tsx` 检测 lanOnly 字段定义/赋值，期望 0 匹配
    - `git log --oneline -1` 标题以 `feat(95-05b):` 开头
  </acceptance_criteria>
  <done>LanOnlyDisclosure 实装 + 测试 PASS + Tooltip/hover fence 落实。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户阅读 RestartBanner / Popover 内容 → 形成对 LAN-only 行为心智 | 文案 i18n-driven (Plan 03)；本 plan 仅做渲染，不引入新文案 |
| RestartBanner.onRestart prop → 父组件（Plan 06）的 invokeWithTrace('restart_app') | onRestart 是 Plan 06 NetworkSection 注入；本组件无直接 IPC 接口 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-05-01 | Information Disclosure | LanOnlyDisclosure 显示文案与 docs/lan-only.md 不一致（营销语 vs 现实 — Pitfall 5） | mitigate | 文案完全来自 Plan 03 i18n 双语契约（已锁定，禁词清单 fence 已落）；本 plan 不写新文案 |
| T-95-05-02 | Tampering | RestartBanner 用 shadcn Alert 替代专用容器导致样式被全局 Alert 风格污染（D-A1 违反） | mitigate | acceptance 含 `不含 from '@/components/ui/alert'` grep fence；测试 Test 8 静态结构断言 |
| T-95-05-03 | UX deception | LanOnlyDisclosure 用 hover Tooltip 替代 click Popover 导致内容容纳不下 + 不可复制（D-C1 违反） | mitigate | acceptance 含 `不含 Tooltip` 与 `不含 onMouseEnter|onHover` grep fence；trigger button 显式 aria-haspopup="dialog" |
| T-95-05-04 | DoS | onRestart 抛错被 RestartBanner 内 catch 静默吞掉导致用户看不到 error | mitigate | 组件不内部 try/catch onRestart — 错误由 Plan 06 NetworkSection 接住后通过 error prop 注入回 Banner；分层职责清晰 |
</threat_model>

<verification>
- RestartBanner 8/8 测试 PASS + LanOnlyDisclosure ≥6/6 测试 PASS
- 全工程 `grep -rnE "from '@/components/ui/alert'|from 'sonner'" src/components/setting/RestartBanner.tsx` 返回 0 匹配（D-A1 fence）
- 全工程 `grep -rnE "Tooltip|onMouseEnter|onHover" src/components/setting/LanOnlyDisclosure.tsx` 返回 0 匹配（D-C1 fence）
- 两个组件 file 不含 `(let|const|var) lanOnly` 字段定义（反向命名规则）
- 渲染 RestartBanner + LanOnlyDisclosure 不破现有测试套件
</verification>

<success_criteria>
- RestartBanner 渲染、交互、a11y、error sub-state 全部覆盖
- LanOnlyDisclosure click-only Popover + 4 类外网请求清单 + a11y trigger 全部覆盖
- 双语 i18n 完全 driven（无硬编码字符串）
- D-A1 / D-A2 / D-A3 / D-B3 / D-C1 / D-C2 决策实装到位
- 3 commits（test+impl Banner → impl Disclosure，因 Disclosure 较简单 RED+GREEN 合并为一 commit 95-05b）
</success_criteria>

<output>
完成后创建 `.planning/phases/095-networksection-ux/095-05-SUMMARY.md`
</output>
